% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_multiparental_dosage_table.R
\name{build_multiparental_dosage}
\alias{build_multiparental_dosage}
\title{Build a Multiparental Dosage Data Frame from Cross Results and Pedigree}
\usage{
build_multiparental_dosage(cross.results, pedigree)
}
\arguments{
\item{cross.results}{A data frame containing cross results. It must include at least the following columns:
\code{parent_1}, \code{parent_2}, \code{individual}, \code{marker}, \code{map_position}, and homolog columns starting with \code{"Homolog_"}.}

\item{pedigree}{A data frame containing pedigree information with columns \code{parent_1}, \code{parent_2}, and \code{individual}.
Individuals with \code{NA} for both \code{parent_1} and \code{parent_2} are considered parents.}
}
\value{
A wide-format data frame with markers as rows and dosage values as columns.
The first columns are \code{marker} and \code{map_position}, followed by the parental dosage columns, and then the remaining individuals.
Missing dosage values are represented as \code{NA}.
}
\description{
This function processes cross results and pedigree information to create a wide-format dosage data frame.
In the output, rows correspond to markers (with their map positions), and columns contain dosage values
for each individual. The function computes a "cross" column for biparental crosses, calculates a dosage column
(summing across homolog columns), splits the data by group (founders or crosses), and then pivots the data
to a wide format. Finally, it reorders the columns so that parental columns (individuals with no recorded
parents in the pedigree) appear first.
}
\examples{
\dontrun{
  # Assuming cross.results and pedigree are defined:
  wide_df <- build_multiparental_dosage(cross.results, pedigree)
}

}
