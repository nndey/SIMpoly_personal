% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polyploid_segregation_utils.R
\name{plot_genotype_heatmap_with_stripe}
\alias{plot_genotype_heatmap_with_stripe}
\title{Plot a heatmap of gamete dosages plus a stripe-bar of dosage proportions}
\usage{
plot_genotype_heatmap_with_stripe(
  gamete_dosage,
  complete_dominance = FALSE,
  col = c("blue", "red")
)
}
\arguments{
\item{gamete_dosage}{Numeric matrix; entry \link{i,j} is parent1_gamete_i + parent2_gamete_j.}

\item{complete_dominance}{Logical; if TRUE, uses two colors (zero vs non-zero), otherwise a gradient.}

\item{col}{Character vector of length 2; c(low_color, high_color) for the fill scale.}
}
\value{
A combined ggplot object (heatmap over stripe bar).
}
\description{
Builds a tile plot of all parental-gamete combinations (cells labeled by dosage),
and below it a bar showing the overall frequency of each dosage.
}
\examples{
# Suppose you have a hexaploid duplex × simplex cross:
p1 <- get_dose_segregation("AAaaaa")
p2 <- get_dose_segregation("AAAAAa")
mat <- outer(p1, p2, "+")
plot_genotype_heatmap_with_stripe(mat, complete_dominance=FALSE, col=c("purple","red"))
}
