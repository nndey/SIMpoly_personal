% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polyploid_segregation_utils.R
\name{segregation}
\alias{segregation}
\title{Simulate segregation, return counts, proportions, and optional plot}
\usage{
segregation(
  geno_p1,
  geno_p2,
  plot = TRUE,
  complete_dominance = FALSE,
  col = c("blue", "red")
)
}
\arguments{
\item{geno_p1}{Character; parent 1 genotype string (even length).}

\item{geno_p2}{Character; parent 2 genotype string (even length).}

\item{plot}{Logical; if TRUE, displays the heatmap + stripe plot.}

\item{complete_dominance}{Logical; passed through to the plotting function.}

\item{col}{Character vector length 2; colors for the fill scale.}
}
\value{
A list with:
\item{genotype_counts}{Named integer vector of counts per dosage.}
\item{expected_segregation}{Numeric vector of proportions per dosage.}
\item{plot}{Invisible ggplot object if \code{plot = TRUE}.}
}
\description{
Crosses two polyploid genotype strings, computes full progeny allele-dosage
distribution, shows a heatmap+stripe plot if requested.
}
\examples{
segregation("AAAAAa", "AAAAAA", plot = TRUE)
}
